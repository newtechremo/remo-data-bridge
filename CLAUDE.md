# Claude Code 프로젝트 규칙

## Git 브랜치 전략 (필수 준수)

### 핵심 규칙
1. **기능 개발 시 반드시 별도 브랜치 생성** (예: `feature/slack-notification`)
2. **main 브랜치에 직접 커밋 절대 금지**
3. **⚠️ main 머지는 사용자가 "머지해", "merge" 등 명시적으로 요청할 때만 수행**
   - 작업 완료 후 자동으로 머지하지 않음
   - 반드시 사용자의 머지 요청을 기다림

### 브랜치 네이밍 규칙
- 기능 추가: `feature/<기능명>`
- 버그 수정: `fix/<버그명>`
- 리팩토링: `refactor/<대상>`

### 워크플로우
1. 기능 개발 요청 시 → 새 브랜치 생성
2. 작업 완료 후 → 브랜치에 커밋 (push는 선택)
3. **사용자가 명시적으로 머지 요청 시에만** → main에 머지
4. 머지 후 push 여부도 사용자에게 확인
